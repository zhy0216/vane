{
  "name": "vane-demo",
  "version": "1.0.0",
  "description": "Standalone demo for Vane email renderer",
  "type": "module",
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "generate": "bun generate.ts"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
